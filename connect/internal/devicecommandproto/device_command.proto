syntax = "proto3";

package devicecommandproto;

message Command {
    string uuid = 1;
    bytes payload = 2;
}

message DeviceCommand {
    string device_udid = 1;
    repeated Command commands = 2;
}
